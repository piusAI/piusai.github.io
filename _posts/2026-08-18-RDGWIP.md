---
layout: post
published: false
title: Render Dependency Graph란?
thumbnail-img: /assets/img/Renderpipeline.jpg
date:   2026-08-018 18:32:00 +0900
description: Unreal engine RDG?
categories:
  - Graphics
tags:
  - Graphics
author: PIUS
---
RDG(Render Dependency Graph)는 Rendering에 필요한 Command와 Resource의 의존성을 그래프 데이터 구조(graph data structure)로 기록한뒤, 이를 한번에 컴파일 하여 실행하는 높은 수준의 렌더링 파이프라인 API이다.

Unreal engine Docs에서 강조하는 RDG의 핵심가치는 다음과 같다
- 메모리 사용 최적화 : Graph 분석을 통해 render Resource 생성, 해제 타이밍을 자동으로 관리하여 VRAM 사용량을 최적화한다.
- 자동 Resource Barrier & Pass 병렬화 : DX12 / Vulkan급 lowlevel resource 상태 전환, CPU/GPU패스간의 의존성을 graph Traversal(순회)로 자동 처리해 작업 병렬성을 극대화한다.

https://dev.epicgames.com/documentation/unreal-engine/render-dependency-graph-in-unreal-engine
UE 공식 문서에서 이야기하는 바이다.

처음 읽을떄 무슨소리인지 몇 번이나 봤지만 Keyword들을 제대로 하나씩 이해해야겠다는 생각을 했다.

##  Shader Parameter Structure

> Shader Parameter Structure System에서는 UE의 매크로처럼 Reflection 시스템에 올릴 수있도록 작성해주어야 한다.
> Dx에서 SetShaderParameter로 HLSL과 시멘틱할수있도록 했다면,
> ShaderParameter Structure를 해주어야한다.

``` hlsl
//hlsl Sourcr File
float2 ViewportSize;
RWTexture2D<float4> SceneColorOutput;
```

```cpp
// ShaderParameter Structure
BEGIN_SHADER_PARAMETER_STRUCT(FMyShaderParameters,)
	SHADER_PARAMETER(FVector2D, ViewportSize)

	SHADER_PARAMETER_UAV(RWTexture2D, SceneColorOutput)
END_SHADER_PARAMETER_STRUCT()
```
Parameter의 이름 잘 맞춰주기!


## First Shader Class?
```cpp
class FMyShaderCS : public FGlobalShader
{
	DECLARE_GLOBAL_SHADER(FMySHaderCS);
	SHADER_USE_PARAMETER_STRUCT(FMyShaderCS, FGlobalShader);
	
	//Engine Template(SetShaderParameters, AddPass 등)이
	// 이 클래스 내부의 parameter Type을 FParameters라는 통일된 이름으로
	// 참조 할 수 있도록 연결!
using FParameters = FMyShaderParameters;
};
```
### 외부에 using(별칭)을 쓰는 이유
1. Header 분리, 모듈화 : 
   Parameter가 점점 커질때, Structure를 Shader Class 외부 / 별도 파일로 분리해서 헤더 가독성 높혀야함
2. 코드 재사용성: VS, PS, ComputeShader가 하나의 Parameter Structure를 공유해야한다면, 외부에서 선언된 structure하나를 각 클래스에서 `using FParameters = 공유 struct`로 불러와 재사용 할 수있다.
3. 단독 사용시 단축 가능 :
   특정 Shader만 단독으로 쓰이는 parameter라면 굳이 외부로 만들지 않고, class 내부에 `BEGIN_SHADER_PARAMETER_STRUCT(FParameters, )`로 즉시 선언하면 using 생략도 가능다른 directory로 Shader에 FParameters유형을 할당



##  ComputeShader?
